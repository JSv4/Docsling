fastapi>=0.100.0,<0.112.0
uvicorn[standard]>=0.20.0,<0.30.0
pydantic>=2.5,<3.0
python-multipart
# Docling and its core dependencies
docling
docling-core
# Other dependencies from the original parser
numpy
pdf2image
pytesseract
pdfplumber
shapely # Includes STRtree
# Potentially needed for file handling if remote storage was used via default_storage
# requests
pydantic-settings>=2.1,<3.0 # For loading settings from env/dotenv
# boto3 # or other relevant storage library 
beam-client